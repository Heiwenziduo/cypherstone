[project]
name = "cypherstone"
version = "0.1.0"
description = "A simple tool to encrypt local files"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cryptography>=46.0.3",
    "customtkinter>=5.2.2",
]
